import AppHeader from '@/components/AppHeader'

describe('AppHeader', () => {
  test('If user not logged in, dont show the button',()=> {
    expect(true).toBe(true)
  })

  test('If user logged in, show the button',()=> {
    expect(true).toBe(true)
  })
})